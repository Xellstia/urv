<%= render "shared/card",
      id: nil, state: nil, kind: :preset,
      title: preset_item.title,
      desc:  preset_item.description,
      minutes: preset_item.minutes_spent,
      actions: "preset_items/card_actions" %>
