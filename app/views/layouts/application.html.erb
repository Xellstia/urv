<!DOCTYPE html>
<html>
  <head>
    <title>URV</title>
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- КРИТИЧЕСКОЕ: устанавливаем состояние сайдбара ДО отрисовки CSS/HTML -->
    <script>
      (function() {
        try {
          if (localStorage.getItem('sidebar:collapsed') === '1') {
            document.documentElement.setAttribute('data-sb', '1'); // узкий режим до первого paint
          }
        } catch (e) { /* no-op */ }
      })();
    </script>

    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50 text-slate-900 antialiased overflow-hidden h-screen">
    <div class="shell h-full flex flex-col" data-controller="sidebar">
      <!-- Топбар статичен на всю ширину -->
      <div class="flex-shrink-0">
        <%= render "shared/topbar" %>
      </div>

      <!-- Фиксированный левый сайдбар под топбаром -->
      <%= render "shared/sidebar" %>

      <!-- Контент с отступом слева = ширина сайдбара, скроллируется -->
      <div class="content-wrap flex-1 min-h-0 overflow-y-auto">
        <main class="min-h-0 min-w-0">
          <%= yield %>
        </main>
      </div>
    </div>

    <%= turbo_frame_tag "modal" %>
  </body>
</html>
