<nav class="h-16 border-b border-slate-800 bg-slate-900 px-4 flex items-center justify-between font-ui text-slate-100 flex-shrink-0">
  <div class="flex items-center gap-3">
    <div class="w-10 h-10 rounded-lg bg-amber-300 text-slate-900 grid place-items-center text-xs font-semibold">
      URV
    </div>

    <!-- Заголовок: первые буквы каждого слова — жёлтые -->
    <span class="font-semibold text-xl whitespace-nowrap">
      <span class="text-amber-300">У</span>чет
      <span class="text-amber-300">Р</span>абочего
      <span class="text-amber-300">В</span>ремени
    </span>
  </div>

  <div class="flex items-center gap-3">
    <% if user_signed_in? %>
      <span class="text-sm text-slate-300 hidden sm:inline"><%= current_user.email %></span>
      <%= link_to destroy_user_session_path, method: :delete,
                  class: "p-3 rounded-lg hover:bg-white/10 flex items-center justify-center",
                  title: "Выйти" do %>
        <%= icon(:logout, classes: "w-8 h-8", aria_label: "Выйти") %>
      <% end %>
    <% end %>
  </div>
</nav>
