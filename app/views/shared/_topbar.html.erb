<nav class="h-12 border-b bg-white px-3 flex items-center justify-between">
  <div class="flex items-center gap-2">
    <!-- Бургер -->
    <button
      type="button"
      class="p-2 rounded hover:bg-slate-100"
      data-action="sidebar#toggle"
      title="Свернуть/развернуть меню"
    >
      <svg class="w-5 h-5 text-slate-600" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <path d="M4 6h16M4 12h16M4 18h16" />
      </svg>
    </button>

    <!-- Лого + название -->
    <div class="w-8 h-8 rounded-lg bg-slate-900/90 text-white grid place-items-center text-xs">URV</div>
    <span class="font-semibold">Учёт времени</span>
  </div>

  <div class="flex items-center gap-3">
    <% if user_signed_in? %>
      <span class="text-sm text-slate-600 hidden sm:inline"><%= current_user.email %></span>
      <%= link_to "Выход", destroy_user_session_path, method: :delete,
            class: "px-3 py-1.5 rounded border hover:bg-slate-50 text-sm" %>
    <% end %>
  </div>
</nav>
